/**
 * @file config.h
 * @brief 系统全局配置文件
 *
 * 包含调试模式、WiFi 凭据、服务端地址及程序版本等配置。
 * 编译前需根据实际环境修改 WiFi 账号密码及服务端地址。
 *
 * 注意：硬件平台（ESP32/ESP8266）由 Arduino 编译工具链根据选中的开发板
 * 自动定义宏，无需在此手动设置。代码中通过 #if (ESP32) / #if defined(ESP8266)
 * 进行平台条件编译。
 */
#ifndef CONFIG_H_
#define CONFIG_H_

// ==================== 硬件平台 ====================
// 平台宏由 Arduino 编译工具链自动定义，无需手动设置：
// - 选择 ESP32 开发板时，编译器自动定义 ESP32 宏
// - 选择 ESP8266 开发板时，编译器自动定义 ESP8266 宏
// 如需切换平台，请在 Arduino IDE 中更换开发板即可。

// ==================== 调试模式 ====================
// 启用后通过串口输出日志信息（波特率 115200）
#define DEBUG_MODE true

// ==================== WiFi 配置 ====================
// WiFi 账号（SSID）
#define WIFI_SSID "ZZ"
// WiFi 密码
#define WIFI_PASSWORD "13581683861"

// ==================== 服务端配置 ====================
// 请求地址前缀（腾讯云函数 IoT 设备接口）
#define URL_PREFIX                                                             \
  "http://xiequ-test.7qbjs.com/watering/api/"

// ==================== 版本信息 ====================
// 程序版本号
#define IOT_VERSION "2.0"

#endif /* CONFIG_H_ */

/**
 * @todo 待办事项
 * - [ ] 确认引脚工作状态
 * - [ ] 服务端识别设备引脚信息
 * - [ ] 服务端支持按钮配置
 * - [ ] 服务端支持延迟关机
 */
